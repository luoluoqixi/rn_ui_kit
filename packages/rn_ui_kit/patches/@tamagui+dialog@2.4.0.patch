# Tamagui Dialog sheet-overlay 补丁说明:
# - 让 Dialog 的 portal 内容通过 @tamagui/portal 当前激活的 portal root host 渲染。
# - 需要与 @tamagui+portal@2.4.0.patch 保持同步。
diff --git a/dist/esm/Dialog.native.js b/dist/esm/Dialog.native.js
index edb3c2cd..ad05e353 100644
--- a/dist/esm/Dialog.native.js
+++ b/dist/esm/Dialog.native.js
@@ -8,7 +8,7 @@ import { createContext } from "@tamagui/create-context";
 import { Dismissable } from "@tamagui/dismissable";
 import { FocusScope, FocusScopeController } from "@tamagui/focus-scope";
 import { composeEventHandlers, withStaticProperties } from "@tamagui/helpers";
-import { needsPortalRepropagation, Portal, PortalItem, resolveViewZIndex } from "@tamagui/portal";
+import { needsPortalRepropagation, Portal, PortalItem, resolveViewZIndex, usePortalRootHostName } from "@tamagui/portal";
 import { RemoveScroll } from "@tamagui/remove-scroll";
 import { SheetController } from "@tamagui/sheet/controller";
 import { ButtonNestingContext, ThemeableStack, YStack } from "@tamagui/stacks";
@@ -93,6 +93,7 @@ var DialogPortalItem = function (param) {
     context,
     children
   } = param;
+  var rootHostName = usePortalRootHostName();
   var themeName = useThemeName();
   var isAdapted = useAdaptIsActive(context.adaptScope);
   var adaptContext = useAdaptContext(context.adaptScope);
@@ -113,7 +114,7 @@ var DialogPortalItem = function (param) {
     scope: context.adaptScope,
     children: content
   }) : context.modal ? /* @__PURE__ */_jsx(PortalItem, {
-    hostName: context.modal ? "root" : context.adaptScope,
+    hostName: context.modal ? rootHostName : context.adaptScope,
     children: content
   }) : content;
 };
