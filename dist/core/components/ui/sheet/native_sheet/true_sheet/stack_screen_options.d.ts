import type { ResolvedColorScheme } from "../../../utils/theme";
import type { TrueSheetInnerStackScreenOptions } from "./stack_navigator";
/** True Sheet 内嵌栈的 screenOptions（仅 iOS Native Stack 使用）。 */
export declare function trueSheetInnerStackScreenOptions(colorScheme: ResolvedColorScheme, backgroundColor: string | undefined, tintColor: string, titleColor: string): TrueSheetInnerStackScreenOptions;
