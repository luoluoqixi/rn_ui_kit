name: Release

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
      - "v[0-9]+.[0-9]+.[0-9]+-*"

permissions:
  contents: write

jobs:
  release:
    name: Create GitHub Release
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout tag and history
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Generate release notes
        run: >-
          node scripts/release/generate-notes.js "$GITHUB_REF_NAME"
          --require-tag --output "$RUNNER_TEMP/release-notes.md"

      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        shell: bash
        run: |
          if gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1; then
            echo "Release $GITHUB_REF_NAME already exists; nothing to do."
            exit 0
          fi

          release_args=(
            "$GITHUB_REF_NAME"
            --verify-tag
            --notes-file "$RUNNER_TEMP/release-notes.md"
            --title "$GITHUB_REF_NAME"
          )

          if [[ "$GITHUB_REF_NAME" == *-* ]]; then
            release_args+=(--prerelease)
          fi

          gh release create "${release_args[@]}"

  android:
    name: Build and upload Android example
    needs: release
    runs-on: ubuntu-24.04
    timeout-minutes: 60

    steps:
      - name: Checkout tag
        uses: actions/checkout@v6

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.2.19

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build and upload release APK
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: bun run release:android-apk "$GITHUB_REF_NAME" --upload
