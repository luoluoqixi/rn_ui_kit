# Android BottomSheetModal 补丁说明:
# - 这个补丁保留了最小范围的 BottomSheetBody opacity guard、Backdrop floor guard，
#   以及 #2690 的 containerHeight 同步尝试，作为历史兼容性缓解措施。
# - 真机测试下，Android 在高频 sheet 开关场景中仍可能复现内容不可见或仅剩 backdrop 的状态。
# - 不应将这个补丁视为可靠根治方案；对于重量级 Android sheet 宿主，更适合逐步迁移
#   离开 @gorhom/bottom-sheet，而不是继续扩大补丁范围。
diff --git a/src/components/bottomSheet/BottomSheetBody.tsx b/src/components/bottomSheet/BottomSheetBody.tsx
index 91b6a6a6..b4b098bf 100644
--- a/src/components/bottomSheet/BottomSheetBody.tsx
+++ b/src/components/bottomSheet/BottomSheetBody.tsx
@@ -12,20 +12,34 @@ type BottomSheetBodyProps = {
 
 function BottomSheetBodyComponent({ style, children }: BottomSheetBodyProps) {
   //#region hooks
-  const { animatedIndex, animatedPosition } = useBottomSheetInternal();
+  const { animatedDetentsState, animatedIndex, animatedPosition } =
+    useBottomSheetInternal();
   //#endregion
 
   //#region styles
   const containerAnimatedStyle = useAnimatedStyle(
-    () => ({
-      opacity: Platform.OS === 'android' && animatedIndex.get() === -1 ? 0 : 1,
-      transform: [
-        {
-          translateY: animatedPosition.get(),
-        },
-      ],
-    }),
-    [animatedPosition, animatedIndex]
+    () => {
+      const closedDetentPosition =
+        animatedDetentsState.get().closedDetentPosition;
+      const isActuallyClosed =
+        closedDetentPosition !== undefined &&
+        Math.abs(animatedPosition.get() - closedDetentPosition) <= 1;
+
+      return {
+        opacity:
+          Platform.OS === 'android' &&
+          animatedIndex.get() === -1 &&
+          isActuallyClosed
+            ? 0
+            : 1,
+        transform: [
+          {
+            translateY: animatedPosition.get(),
+          },
+        ],
+      };
+    },
+    [animatedPosition, animatedIndex, animatedDetentsState]
   );
   const containerStyle = useMemo(
     () => [style, styles.container, containerAnimatedStyle],
diff --git a/src/components/bottomSheet/BottomSheet.tsx b/src/components/bottomSheet/BottomSheet.tsx
index fc520337..26ee4ecc 100644
--- a/src/components/bottomSheet/BottomSheet.tsx
+++ b/src/components/bottomSheet/BottomSheet.tsx
@@ -1811,6 +1811,7 @@ const BottomSheetComponent = forwardRef<BottomSheet, BottomSheetProps>(
               containerLayoutState={containerLayoutState}
               topInset={topInset}
               bottomInset={bottomInset}
+              modal={$modal}
               detached={detached}
               style={_providedContainerStyle}
             >
diff --git a/src/components/bottomSheetHostingContainer/BottomSheetHostingContainer.tsx b/src/components/bottomSheetHostingContainer/BottomSheetHostingContainer.tsx
index 879061de..5f563908 100644
--- a/src/components/bottomSheetHostingContainer/BottomSheetHostingContainer.tsx
+++ b/src/components/bottomSheetHostingContainer/BottomSheetHostingContainer.tsx
@@ -17,6 +17,7 @@ function BottomSheetHostingContainerComponent({
   layoutState,
   topInset = 0,
   bottomInset = 0,
+  modal,
   shouldCalculateHeight = true,
   detached,
   style,
@@ -61,6 +62,7 @@ function BottomSheetHostingContainerComponent({
         layoutState.modify(state => {
           'worklet';
           state.rawContainerHeight = height;
+          state.containerHeight = modal ? height - topInset - bottomInset : height;
           return state;
         });
       }
@@ -108,7 +110,7 @@ function BottomSheetHostingContainerComponent({
         });
       }
     },
-    [layoutState, containerLayoutState]
+    [layoutState, containerLayoutState, modal, topInset, bottomInset]
   );
   //#endregion
 
diff --git a/src/components/bottomSheetHostingContainer/types.d.ts b/src/components/bottomSheetHostingContainer/types.d.ts
index ed43773f..80d84662 100644
--- a/src/components/bottomSheetHostingContainer/types.d.ts
+++ b/src/components/bottomSheetHostingContainer/types.d.ts
@@ -10,6 +10,7 @@ export interface BottomSheetHostingContainerProps
   > {
   containerLayoutState?: SharedValue<ContainerLayoutState>;
   layoutState?: SharedValue<LayoutState>;
+  modal?: boolean;
 
   shouldCalculateHeight?: boolean;
   style?: StyleProp<ViewStyle>;
diff --git a/src/components/bottomSheetBackdrop/BottomSheetBackdrop.tsx b/src/components/bottomSheetBackdrop/BottomSheetBackdrop.tsx
index a0345b01..3bd181cb 100644
--- a/src/components/bottomSheetBackdrop/BottomSheetBackdrop.tsx
+++ b/src/components/bottomSheetBackdrop/BottomSheetBackdrop.tsx
@@ -120,7 +120,7 @@ const BottomSheetBackdropComponent = ({
 
   //#region effects
   useAnimatedReaction(
-    () => animatedIndex.value <= disappearsOnIndex,
+    () => Math.floor(animatedIndex.value) <= disappearsOnIndex,
     (shouldDisableTouchability, previous) => {
       if (shouldDisableTouchability === previous) {
         return;
