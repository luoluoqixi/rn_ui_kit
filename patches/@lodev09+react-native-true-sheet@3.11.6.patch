# TrueSheet 补丁：
# - iOS：保持 TrueSheet scroll indicator 的底部 inset 不小于 TrueSheet 自己的 bottom inset。
# - iOS：支持通过 native tag 显式绑定 ScrollView；未绑定时保持原有两层自动查找。
# - iOS：扩大 custom native grabber 的透明整行拖拽热区，同时保持 pill 的可见大小不变。
# - Android：扩大 native grabber 的透明整行拖拽热区，并让该区域强制走 sheet drag。
# - Android：增加 disableStackingTranslation 开关，可禁用子 sheet 打开时父 sheet 下移的 stacked translation。
# - Android：Navigation stack 内 scrollable 页面到顶后继续下拉时，允许从 active scrollView 把手势接回 sheet。
# - Android：ScrollView 内手势默认归还给子列表，避免 release 下偏右侧滚动被 BottomSheetBehavior 默认拦截误拖动 sheet。
# - Android：下调 TrueSheet 原生 dismiss 判定门槛，减少下拉关闭时的回弹。
# - Android：fit + scrollable 打开动画期间改用稳定测量高度，避免 auto detent 因中间态高度回写而反复重布局卡顿。
diff --git a/android/src/main/java/com/lodev09/truesheet/TrueSheetContentView.kt b/android/src/main/java/com/lodev09/truesheet/TrueSheetContentView.kt
index 5fe2e2c..4c3c0d5 100644
--- a/android/src/main/java/com/lodev09/truesheet/TrueSheetContentView.kt
+++ b/android/src/main/java/com/lodev09/truesheet/TrueSheetContentView.kt
@@ -83,6 +83,26 @@ class TrueSheetContentView(private val reactContext: ThemedReactContext) : React
     }
   }
 
+  private fun pinScrollView(scrollView: ViewGroup) {
+    originalScrollViewPaddingBottom = scrollView.paddingBottom
+    pinnedScrollView = scrollView
+
+    scrollView.isNestedScrollingEnabled = true
+    (scrollView.parent as? SwipeRefreshLayout)?.isNestedScrollingEnabled = false
+
+    scrollView.setOnScrollChangeListener { _, _, scrollY, _, oldScrollY ->
+      if (scrollY != oldScrollY) {
+        delegate?.contentViewDidScroll()
+      }
+    }
+  }
+
+  private fun syncPinnedScrollViewInset() {
+    val keyboardHeight = keyboardObserver?.currentHeight ?: 0
+    val basePadding = if (keyboardHeight > 0) keyboardHeight else bottomInset
+    setScrollViewPaddingBottom(originalScrollViewPaddingBottom + basePadding)
+  }
+
   fun setupScrollable(enabled: Boolean, bottomInset: Int) {
     if (!enabled) {
       clearScrollable()
@@ -94,37 +114,23 @@ class TrueSheetContentView(private val reactContext: ThemedReactContext) : React
       clearScrollable()
     }
 
-    // Already set up with same inset and valid scroll view
-    if (pinnedScrollView != null && this.bottomInset == bottomInset) {
-      return
-    }
-
     val scrollView = findScrollView(this) ?: return
 
-    // Only capture originals on first pin
-    if (pinnedScrollView == null) {
-      originalScrollViewPaddingBottom = scrollView.paddingBottom
-      pinnedScrollView = scrollView
-
-      scrollView.isNestedScrollingEnabled = true
-      (scrollView.parent as? SwipeRefreshLayout)?.isNestedScrollingEnabled = false
+    // Already set up with same inset and still pinned to the current visible scroll view
+    if (pinnedScrollView === scrollView && this.bottomInset == bottomInset) {
+      return
+    }
 
-      scrollView.setOnScrollChangeListener { _, _, scrollY, _, oldScrollY ->
-        if (scrollY != oldScrollY) {
-          delegate?.contentViewDidScroll()
-        }
-      }
+    if (pinnedScrollView !== scrollView) {
+      clearScrollable()
+      pinScrollView(scrollView)
+    } else if (pinnedScrollView == null) {
+      pinScrollView(scrollView)
     }
 
     this.bottomInset = bottomInset
 
-    setScrollViewPaddingBottom(originalScrollViewPaddingBottom + bottomInset)
-
-    // If keyboard is currently showing, re-apply the keyboard inset to the new ScrollView
-    val keyboardHeight = keyboardObserver?.currentHeight ?: 0
-    if (keyboardHeight > 0) {
-      setScrollViewPaddingBottom(originalScrollViewPaddingBottom + keyboardHeight)
-    }
+    syncPinnedScrollViewInset()
   }
 
   // TODO: Replace this workaround with synchronous state layout updates on every sheet resize.
@@ -163,17 +169,40 @@ class TrueSheetContentView(private val reactContext: ThemedReactContext) : React
   }
 
   fun findScrollView(): ViewGroup? {
-    if (pinnedScrollView != null) return pinnedScrollView
-    return findScrollView(this as View)
+    val visibleScrollView = findScrollView(this as View)
+
+    if (visibleScrollView != null && pinnedScrollView !== visibleScrollView) {
+      clearScrollable()
+      pinScrollView(visibleScrollView)
+      syncPinnedScrollViewInset()
+    }
+
+    if (
+      pinnedScrollView != null &&
+      pinnedScrollView?.isDescendantOf(this) == true &&
+      pinnedScrollView?.isShown == true
+    ) {
+      return pinnedScrollView
+    }
+
+    if (pinnedScrollView != null) {
+      clearScrollable()
+    }
+
+    return visibleScrollView
   }
 
   private fun findScrollView(view: View): ViewGroup? {
+    if (!view.isShown) {
+      return null
+    }
+
     if (view is ScrollView || view is NestedScrollView) {
       return view as ViewGroup
     }
 
     if (view is ViewGroup) {
-      for (i in 0 until view.childCount) {
+      for (i in view.childCount - 1 downTo 0) {
         val scrollView = findScrollView(view.getChildAt(i))
         if (scrollView != null) {
           return scrollView
@@ -302,8 +331,4 @@ class TrueSheetContentView(private val reactContext: ThemedReactContext) : React
         scrollView.smoothScrollTo(0, (caretTop - offset).coerceAtLeast(0))
     }
   }
-
-  companion object {
-    const val TAG_NAME = "TrueSheet"
-  }
 }
diff --git a/android/src/main/java/com/lodev09/truesheet/TrueSheetView.kt b/android/src/main/java/com/lodev09/truesheet/TrueSheetView.kt
index 6f03b91..5da07ac 100644
--- a/android/src/main/java/com/lodev09/truesheet/TrueSheetView.kt
+++ b/android/src/main/java/com/lodev09/truesheet/TrueSheetView.kt
@@ -43,6 +43,7 @@ class TrueSheetView(private val reactContext: ThemedReactContext) :
   // Initial present configuration (set by ViewManager before mount)
   var initialDetentIndex: Int = -1
   var initialDetentAnimated: Boolean = true
+  internal var disableStackingTranslation: Boolean = false
   private var didInitiallyPresent: Boolean = false
 
   private var lastContainerWidth: Int = 0
@@ -253,6 +254,10 @@ class TrueSheetView(private val reactContext: ThemedReactContext) :
     viewController.draggable = draggable
   }
 
+  fun setDisableStackingTranslation(disableStackingTranslation: Boolean) {
+    this.disableStackingTranslation = disableStackingTranslation
+  }
+
   fun setGrabber(grabber: Boolean) {
     viewController.grabber = grabber
   }
@@ -455,7 +460,7 @@ class TrueSheetView(private val reactContext: ThemedReactContext) :
    * Propagates additional translation to parent so the entire stack stays visually consistent.
    */
   fun updateTranslationForChild(childSheetTop: Int) {
-    if (!viewController.isSheetVisible || viewController.isExpanded) return
+    if (disableStackingTranslation || !viewController.isSheetVisible || viewController.isExpanded) return
 
     viewController.sheetView?.behavior?.isDraggable = false
 
@@ -475,7 +480,7 @@ class TrueSheetView(private val reactContext: ThemedReactContext) :
    * Recursively adds translation to this sheet and all parent sheets.
    */
   private fun addTranslation(amount: Int) {
-    if (viewController.isExpanded) return
+    if (disableStackingTranslation || viewController.isExpanded) return
 
     viewController.translateSheet(viewController.currentTranslationY + amount)
     TrueSheetStackManager.getParentSheet(this)?.addTranslation(amount)
@@ -490,6 +495,8 @@ class TrueSheetView(private val reactContext: ThemedReactContext) :
     viewController.translateSheet(0, onEnd = onTranslateEnd)
 
     // Parent should recalculate its translation based on this sheet's position
+    if (disableStackingTranslation) return
+
     val mySheetTop = viewController.detentCalculator.getSheetTopForDetentIndex(viewController.currentDetentIndex)
     TrueSheetStackManager.getParentSheet(this)?.updateTranslationForChild(mySheetTop)
   }
@@ -565,7 +572,7 @@ class TrueSheetView(private val reactContext: ThemedReactContext) :
     // On android scrollable, we need the actual sheet height to get proper ScrollView height.
     // Unlike IOS where ScrollView is pinned to the container.
     val effectiveHeight = if (viewController.scrollable) height else viewController.screenHeight
-    updateState(width, effectiveHeight)
+    updateState(width, if (viewController.shouldUseScreenHeightForScrollableFitMeasure()) viewController.screenHeight else effectiveHeight)
   }
 
   override fun viewControllerWillFocus() {
diff --git a/android/src/main/java/com/lodev09/truesheet/TrueSheetViewController.kt b/android/src/main/java/com/lodev09/truesheet/TrueSheetViewController.kt
index 88d340d..fd9229c 100644
--- a/android/src/main/java/com/lodev09/truesheet/TrueSheetViewController.kt
+++ b/android/src/main/java/com/lodev09/truesheet/TrueSheetViewController.kt
@@ -119,6 +119,8 @@ class TrueSheetViewController(private val reactContext: ThemedReactContext) :
     private const val TRANSLATE_ANIMATION_DURATION = 200L
     private const val DISMISS_DURATION = 200L
     private const val SCREEN_FADE_DURATION = 150L
+    private const val ANDROID_HIDE_FRICTION = 0.3f
+    private const val ANDROID_SIGNIFICANT_VELOCITY_THRESHOLD = 200
   }
 
   // =============================================================================
@@ -347,6 +349,9 @@ class TrueSheetViewController(private val reactContext: ThemedReactContext) :
 
   fun isDimmedAtDetentIndex(index: Int): Boolean = dimmed && index >= dimmedDetentIndex
 
+  internal fun shouldUseScreenHeightForScrollableFitMeasure(): Boolean =
+    scrollable && isPresentAnimating && detents.size == 1 && detents.firstOrNull() == -1.0
+
   // =============================================================================
   // MARK: - Sheet Creation & Cleanup
   // =============================================================================
@@ -756,6 +761,11 @@ class TrueSheetViewController(private val reactContext: ThemedReactContext) :
     behavior.isHideable = true
     behavior.isDraggable = draggable
     behavior.scrollingExpandsSheet = scrollableOptions?.scrollingExpandsSheet ?: true
+    // Material default dismissal tuning on Android is fairly conservative for
+    // modal sheets. Nudging these closer to gorhom-style momentum makes
+    // drag-to-close feel less sticky without changing detent mapping.
+    behavior.setHideFriction(ANDROID_HIDE_FRICTION)
+    behavior.setSignificantVelocityThreshold(ANDROID_SIGNIFICANT_VELOCITY_THRESHOLD)
     behavior.state = BottomSheetBehavior.STATE_HIDDEN
     behavior.addBottomSheetCallback(sheetCallback)
 
diff --git a/android/src/main/java/com/lodev09/truesheet/TrueSheetViewManager.kt b/android/src/main/java/com/lodev09/truesheet/TrueSheetViewManager.kt
index 35c265a..95cdc9e 100644
--- a/android/src/main/java/com/lodev09/truesheet/TrueSheetViewManager.kt
+++ b/android/src/main/java/com/lodev09/truesheet/TrueSheetViewManager.kt
@@ -146,6 +146,11 @@ class TrueSheetViewManager :
     view.setDraggable(draggable)
   }
 
+  @ReactProp(name = "disableStackingTranslation", defaultBoolean = false)
+  override fun setDisableStackingTranslation(view: TrueSheetView, disableStackingTranslation: Boolean) {
+    view.setDisableStackingTranslation(disableStackingTranslation)
+  }
+
   @ReactProp(name = "dimmed", defaultBoolean = true)
   override fun setDimmed(view: TrueSheetView, dimmed: Boolean) {
     view.setDimmed(dimmed)
diff --git a/android/src/main/java/com/lodev09/truesheet/core/TrueSheetBottomSheetView.kt b/android/src/main/java/com/lodev09/truesheet/core/TrueSheetBottomSheetView.kt
index 11f43c2..29182f5 100644
--- a/android/src/main/java/com/lodev09/truesheet/core/TrueSheetBottomSheetView.kt
+++ b/android/src/main/java/com/lodev09/truesheet/core/TrueSheetBottomSheetView.kt
@@ -212,6 +212,17 @@ class TrueSheetBottomSheetView(private val reactContext: ThemedReactContext) : F
     findViewWithTag<TrueSheetGrabberView>(GRABBER_TAG)?.updateAccessibilityValue(index, detentCount)
   }
 
+  fun isTouchInGrabber(rawX: Int, rawY: Int): Boolean {
+    val grabber = findViewWithTag<View>(GRABBER_TAG) ?: return false
+    val location = IntArray(2)
+    grabber.getLocationOnScreen(location)
+
+    return rawX >= location[0] &&
+      rawX <= location[0] + grabber.width &&
+      rawY >= location[1] &&
+      rawY <= location[1] + grabber.height
+  }
+
   // =============================================================================
   // MARK: - Grabber Tap Detection
   // =============================================================================
diff --git a/android/src/main/java/com/lodev09/truesheet/core/TrueSheetCoordinatorLayout.kt b/android/src/main/java/com/lodev09/truesheet/core/TrueSheetCoordinatorLayout.kt
index b4b5d60..57b0f91 100644
--- a/android/src/main/java/com/lodev09/truesheet/core/TrueSheetCoordinatorLayout.kt
+++ b/android/src/main/java/com/lodev09/truesheet/core/TrueSheetCoordinatorLayout.kt
@@ -8,7 +8,9 @@ import android.view.View
 import android.view.ViewConfiguration
 import android.view.ViewGroup
 import android.widget.EditText
+import android.widget.ScrollView
 import androidx.coordinatorlayout.widget.CoordinatorLayout
+import androidx.core.widget.NestedScrollView
 import androidx.swiperefreshlayout.widget.SwipeRefreshLayout
 import com.facebook.react.uimanager.PointerEvents
 import com.facebook.react.uimanager.ReactPointerEventsView
@@ -43,8 +45,14 @@ class TrueSheetCoordinatorLayout(context: Context) :
   // interception once the first significant movement of a stream is horizontal.
   private var streamInitialX = 0f
   private var streamInitialY = 0f
+  private var streamInitialLocalX = 0f
+  private var streamInitialLocalY = 0f
   private var streamHorizontalLocked = false
   private var streamDirectionDecided = false
+  private var streamStartedInGrabber = false
+  private var streamStartedInScrollView = false
+  private var streamScrollView: ViewGroup? = null
+  private var streamScrollViewChain: List<ViewGroup> = emptyList()
 
   // Native-gesture-claim tracking. RNGH handlers activate past BottomSheetBehavior's
   // drag slop and never call requestDisallowInterceptTouchEvent, so the sheet would
@@ -111,6 +119,7 @@ class TrueSheetCoordinatorLayout(context: Context) :
     // (a pinned ScrollView or an overflowing multiline EditText still scrolls). Other children
     // (maps, sliders, scrollables) keep the standard disallow contract.
     if (disallowIntercept && touchedViewIsEditText() && !touchedViewCanScrollVertically()) return
+    if (disallowIntercept && shouldIgnoreDisallowInterceptForTopScrollView()) return
     super.requestDisallowInterceptTouchEvent(disallowIntercept)
   }
 
@@ -138,6 +147,96 @@ class TrueSheetCoordinatorLayout(context: Context) :
     return false
   }
 
+  private fun shouldIgnoreDisallowInterceptForTopScrollView(): Boolean {
+    val scrollView = streamScrollView ?: delegate?.findScrollView() ?: return false
+    val scrollViews = streamScrollViewChain.ifEmpty { listOf(scrollView) }
+    val hasRefreshControl = scrollViews.any { it.parent is SwipeRefreshLayout }
+    if (hasRefreshControl || scrollViews.any { it.canScrollVertically(-1) }) return false
+    return findDescendantAt(scrollView, streamInitialX.toInt(), streamInitialY.toInt()) { true }
+  }
+
+  private fun shouldPreferSheetTopPull(ev: MotionEvent): Boolean {
+    val scrollView = streamScrollView ?: delegate?.findScrollView() ?: return false
+    val scrollViews = streamScrollViewChain.ifEmpty { listOf(scrollView) }
+    val hasRefreshControl = scrollViews.any { it.parent is SwipeRefreshLayout }
+    if (
+      !streamStartedInScrollView ||
+      hasRefreshControl ||
+      scrollViews.any { it.canScrollVertically(-1) }
+    ) return false
+
+    val pointerIndex = ev.findPointerIndex(activePointerId)
+    if (pointerIndex == -1) return false
+
+    val deltaY = initialY - ev.getY(pointerIndex)
+    return deltaY < -touchSlop
+  }
+
+  /**
+   * Returns the deepest vertical ScrollView under this touch.
+   *
+   * TrueSheet pins one page-level ScrollView, but a page can contain another
+   * independently scrolling view. Gesture ownership must follow the view that
+   * was actually touched; otherwise an outer ScrollView at offset 0 makes the
+   * sheet steal a downward drag while the inner ScrollView is still scrolled.
+   */
+  private fun findDeepestScrollViewAt(view: View, rawX: Int, rawY: Int): ViewGroup? {
+    val location = IntArray(2)
+    view.getLocationOnScreen(location)
+    if (
+      rawX < location[0] ||
+      rawX > location[0] + view.width ||
+      rawY < location[1] ||
+      rawY > location[1] + view.height
+    ) {
+      return null
+    }
+
+    if (view is ViewGroup) {
+      for (i in view.childCount - 1 downTo 0) {
+        val scrollView = findDeepestScrollViewAt(view.getChildAt(i), rawX, rawY)
+        if (scrollView != null) return scrollView
+      }
+    }
+
+    return if (view is ScrollView || view is NestedScrollView) view as ViewGroup else null
+  }
+
+  /**
+   * Returns the touched ScrollView followed by each scrolling ancestor up to
+   * TrueSheet's pinned page ScrollView. The sheet may handle a top pull only
+   * after every view in this chain has reached its top edge.
+   */
+  private fun buildScrollViewChain(
+    scrollView: ViewGroup,
+    boundaryScrollView: ViewGroup
+  ): List<ViewGroup> {
+    val chain = mutableListOf<ViewGroup>()
+    var current: View? = scrollView
+
+    while (current != null) {
+      if (current is ScrollView || current is NestedScrollView) {
+        chain.add(current as ViewGroup)
+      }
+      if (current === boundaryScrollView) break
+      current = current.parent as? View
+    }
+
+    if (chain.none { it === boundaryScrollView }) {
+      chain.add(boundaryScrollView)
+    }
+    return chain
+  }
+
+  private fun primeSheetDragFromInitialTouch(ev: MotionEvent) {
+    val downEvent = MotionEvent.obtain(ev).apply {
+      action = MotionEvent.ACTION_DOWN
+      setLocation(streamInitialLocalX, streamInitialLocalY)
+    }
+    super.onInterceptTouchEvent(downEvent)
+    downEvent.recycle()
+  }
+
   /**
    * Called by descendant RootView implementations (controller, footer) when a child
    * starts a native gesture — e.g. an RNGH handler activated or a scrollable began
@@ -162,12 +261,32 @@ class TrueSheetCoordinatorLayout(context: Context) :
 
       streamInitialX = ev.rawX
       streamInitialY = ev.rawY
+      streamInitialLocalX = ev.x
+      streamInitialLocalY = ev.y
       streamHorizontalLocked = false
       streamDirectionDecided = false
       streamGestureClaimed = false
-      streamHasGestureRoot = findDescendantAt(this, ev.rawX.toInt(), ev.rawY.toInt()) {
-        it.javaClass.name == GESTURE_HANDLER_ROOT_VIEW_CLASS
+      streamStartedInGrabber = delegate?.findSheetView()?.isTouchInGrabber(ev.rawX.toInt(), ev.rawY.toInt()) == true
+      val initialScrollView = delegate?.findScrollView()
+      streamScrollView = initialScrollView?.let {
+        findDeepestScrollViewAt(it, streamInitialX.toInt(), streamInitialY.toInt())
+      }
+      streamScrollViewChain = if (initialScrollView != null && streamScrollView != null) {
+        buildScrollViewChain(streamScrollView!!, initialScrollView)
+      } else {
+        emptyList()
       }
+      streamStartedInScrollView = streamScrollView != null
+      initialY = ev.y
+      activePointerId = ev.getPointerId(0)
+      if (streamStartedInGrabber) {
+        dragging = false
+      }
+      streamHasGestureRoot =
+        !streamStartedInGrabber &&
+          findDescendantAt(this, ev.rawX.toInt(), ev.rawY.toInt()) {
+            it.javaClass.name == GESTURE_HANDLER_ROOT_VIEW_CLASS
+          }
     }
 
     // Decide gesture direction on first significant movement of the stream.
@@ -186,13 +305,27 @@ class TrueSheetCoordinatorLayout(context: Context) :
       if (action == MotionEvent.ACTION_UP || action == MotionEvent.ACTION_CANCEL) {
         streamHorizontalLocked = false
         streamDirectionDecided = false
+        streamStartedInGrabber = false
+        streamStartedInScrollView = false
+        streamScrollView = null
+        streamScrollViewChain = emptyList()
       }
       return false
     }
 
+    if (streamGestureClaimed) {
+      if (action == MotionEvent.ACTION_MOVE && shouldPreferSheetTopPull(ev)) {
+        streamGestureClaimed = false
+      }
+    }
+
     if (streamGestureClaimed) {
       if (action == MotionEvent.ACTION_UP || action == MotionEvent.ACTION_CANCEL) {
         streamGestureClaimed = false
+        streamStartedInGrabber = false
+        streamStartedInScrollView = false
+        streamScrollView = null
+        streamScrollViewChain = emptyList()
       }
       return false
     }
@@ -201,19 +334,54 @@ class TrueSheetCoordinatorLayout(context: Context) :
     // under the touch can activate and claim the stream first.
     if (streamHasGestureRoot &&
       action == MotionEvent.ACTION_MOVE &&
+      !shouldPreferSheetTopPull(ev) &&
       kotlin.math.abs(ev.rawY - streamInitialY) < touchSlop * GESTURE_CLAIM_SLOP_FACTOR
     ) {
       return false
     }
 
-    val scrollView = delegate?.findScrollView()
-    val hasRefreshControl = scrollView?.parent is SwipeRefreshLayout
-    val cannotScroll = scrollView != null &&
+    if (streamStartedInGrabber) {
+      when (ev.action and MotionEvent.ACTION_MASK) {
+        MotionEvent.ACTION_MOVE -> {
+          val pointerIndex = ev.findPointerIndex(activePointerId)
+          if (pointerIndex != -1) {
+            val y = ev.getY(pointerIndex)
+            val deltaY = initialY - y
+            if (kotlin.math.abs(deltaY) > touchSlop) {
+              dragging = true
+              parent?.requestDisallowInterceptTouchEvent(true)
+            }
+          }
+        }
+
+        MotionEvent.ACTION_UP,
+        MotionEvent.ACTION_CANCEL -> {
+          dragging = false
+          streamStartedInGrabber = false
+          streamStartedInScrollView = false
+          streamScrollView = null
+          streamScrollViewChain = emptyList()
+        }
+      }
+
+      if (dragging) return true
+      return super.onInterceptTouchEvent(ev)
+    }
+
+    val scrollView = streamScrollView ?: delegate?.findScrollView()
+    val scrollViews = if (streamScrollViewChain.isNotEmpty()) {
+      streamScrollViewChain
+    } else {
+      listOfNotNull(scrollView)
+    }
+    val hasRefreshControl = scrollViews.any { it.parent is SwipeRefreshLayout }
+    val canPullSheetFromTop = scrollView != null &&
+      streamStartedInScrollView &&
       !hasRefreshControl &&
-      scrollView.scrollY == 0 &&
-      !scrollView.canScrollVertically(1)
+      scrollViews.none { it.canScrollVertically(-1) }
+    val cannotScroll = canPullSheetFromTop && scrollViews.none { it.canScrollVertically(1) }
 
-    if (cannotScroll) {
+    if (canPullSheetFromTop) {
       when (ev.action and MotionEvent.ACTION_MASK) {
         MotionEvent.ACTION_DOWN -> {
           dragging = false
@@ -226,7 +394,17 @@ class TrueSheetCoordinatorLayout(context: Context) :
           if (pointerIndex != -1) {
             val y = ev.getY(pointerIndex)
             val deltaY = initialY - y
-            if (kotlin.math.abs(deltaY) > touchSlop) {
+            val shouldStartDragging =
+              if (cannotScroll) {
+                kotlin.math.abs(deltaY) > touchSlop
+              } else {
+                // ScrollView 已在顶部时，仅当用户继续向下拖拽才把手势交回 sheet，
+                // 保持向上滚动仍由列表自己处理。
+                deltaY < -touchSlop
+              }
+
+            if (shouldStartDragging) {
+              primeSheetDragFromInitialTouch(ev)
               dragging = true
               parent?.requestDisallowInterceptTouchEvent(true)
             }
@@ -236,13 +414,25 @@ class TrueSheetCoordinatorLayout(context: Context) :
         MotionEvent.ACTION_UP,
         MotionEvent.ACTION_CANCEL -> {
           dragging = false
+          streamStartedInGrabber = false
+          streamStartedInScrollView = false
+          streamScrollView = null
+          streamScrollViewChain = emptyList()
         }
       }
     } else {
       dragging = false
     }
 
-    return dragging || super.onInterceptTouchEvent(ev)
+    if (dragging) return true
+
+    // If this stream began inside the active ScrollView, let the child keep it
+    // unless the explicit top-pull handoff above has already started dragging the sheet.
+    if (streamStartedInScrollView && scrollView != null) {
+      return false
+    }
+
+    return super.onInterceptTouchEvent(ev)
   }
 
   @SuppressLint("ClickableViewAccessibility")
@@ -252,6 +442,8 @@ class TrueSheetCoordinatorLayout(context: Context) :
         MotionEvent.ACTION_UP,
         MotionEvent.ACTION_CANCEL -> {
           dragging = false
+          streamScrollView = null
+          streamScrollViewChain = emptyList()
         }
       }
       // Let parent CoordinatorLayout handle the touch for BottomSheetBehavior
diff --git a/android/src/main/java/com/lodev09/truesheet/core/TrueSheetGrabberView.kt b/android/src/main/java/com/lodev09/truesheet/core/TrueSheetGrabberView.kt
index c14a0e0..282cea4 100644
--- a/android/src/main/java/com/lodev09/truesheet/core/TrueSheetGrabberView.kt
+++ b/android/src/main/java/com/lodev09/truesheet/core/TrueSheetGrabberView.kt
@@ -8,6 +8,7 @@ import android.graphics.drawable.GradientDrawable
 import android.os.Bundle
 import android.view.Gravity
 import android.view.View
+import android.view.ViewGroup
 import android.view.accessibility.AccessibilityNodeInfo
 import android.widget.FrameLayout
 import androidx.core.graphics.ColorUtils
@@ -38,8 +39,8 @@ class TrueSheetGrabberView(context: Context, private val options: GrabberOptions
     private const val DEFAULT_HEIGHT = 4f // dp
     private const val DEFAULT_TOP_MARGIN = 16f // dp
     private const val DEFAULT_ALPHA = 0.4f
-    private const val HITBOX_PADDING_HORIZONTAL = 16f // dp
     private const val HITBOX_PADDING_VERTICAL = 16f // dp
+    private const val MINIMUM_HIT_HEIGHT = 28f // dp
     private val DEFAULT_COLOR = Color.argb((DEFAULT_ALPHA * 255).toInt(), 73, 69, 79) // #49454F @ 40%
   }
 
@@ -65,15 +66,13 @@ class TrueSheetGrabberView(context: Context, private val options: GrabberOptions
   var onAccessibilityDecrement: (() -> Unit)? = null
 
   init {
-    val hitboxWidth = grabberWidth + (HITBOX_PADDING_HORIZONTAL * 2)
-    val hitboxHeight = grabberHeight + (HITBOX_PADDING_VERTICAL * 2)
+    val hitboxHeight = maxOf(MINIMUM_HIT_HEIGHT, grabberHeight + grabberTopMargin + HITBOX_PADDING_VERTICAL)
 
     layoutParams = LayoutParams(
-      hitboxWidth.dpToPx().toInt(),
+      ViewGroup.LayoutParams.MATCH_PARENT,
       hitboxHeight.dpToPx().toInt()
     ).apply {
       gravity = Gravity.CENTER_HORIZONTAL or Gravity.TOP
-      topMargin = (grabberTopMargin - HITBOX_PADDING_VERTICAL).dpToPx().toInt()
     }
 
     // The visible pill centered inside the hitbox
@@ -82,7 +81,8 @@ class TrueSheetGrabberView(context: Context, private val options: GrabberOptions
         grabberWidth.dpToPx().toInt(),
         grabberHeight.dpToPx().toInt()
       ).apply {
-        gravity = Gravity.CENTER
+        gravity = Gravity.CENTER_HORIZONTAL or Gravity.TOP
+        topMargin = grabberTopMargin.dpToPx().toInt()
       }
       background = GradientDrawable().apply {
         shape = GradientDrawable.RECTANGLE
diff --git a/android/src/main/java/com/lodev09/truesheet/core/TrueSheetStackManager.kt b/android/src/main/java/com/lodev09/truesheet/core/TrueSheetStackManager.kt
index c744b19..0e33b72 100644
--- a/android/src/main/java/com/lodev09/truesheet/core/TrueSheetStackManager.kt
+++ b/android/src/main/java/com/lodev09/truesheet/core/TrueSheetStackManager.kt
@@ -63,10 +63,14 @@ object TrueSheetStackManager {
    */
   @JvmStatic
   fun updateParentTranslation(sheetView: TrueSheetView) {
+    if (sheetView.disableStackingTranslation) return
+
     synchronized(presentedSheetStack) {
       val index = presentedSheetStack.indexOf(sheetView)
       val parentSheet = getParentSheetAt(index, sheetView.rootContainerView) ?: return
 
+      if (parentSheet.disableStackingTranslation) return
+
       // Post to ensure layout is complete before reading position
       sheetView.viewController.post {
         val childMinSheetTop = sheetView.viewController.detentCalculator.getSheetTopForDetentIndex(0)
diff --git a/ios/TrueSheetContentView.mm b/ios/TrueSheetContentView.mm
index a5c43ed..c62178d 100644
--- a/ios/TrueSheetContentView.mm
+++ b/ios/TrueSheetContentView.mm
@@ -1,382 +1,455 @@
 //
 //  Created by Jovanni Lo (@lodev09)
 //  Copyright (c) 2024-present. All rights reserved.
 //
 //  This source code is licensed under the MIT license found in the
 //  LICENSE file in the root directory of this source tree.
 //
 
 #ifdef RCT_NEW_ARCH_ENABLED
 
 #import "TrueSheetContentView.h"
 #import <React/RCTScrollViewComponentView.h>
 #import <react/renderer/components/TrueSheetSpec/ComponentDescriptors.h>
 #import <react/renderer/components/TrueSheetSpec/EventEmitters.h>
 #import <react/renderer/components/TrueSheetSpec/Props.h>
 #import <react/renderer/components/TrueSheetSpec/RCTComponentViewHelpers.h>
 #import "TrueSheetContainerView.h"
 #import "TrueSheetView.h"
 #import "TrueSheetViewController.h"
 #import "utils/PlatformUtil.h"
 #import "utils/UIView+FirstResponder.h"
 
 using namespace facebook::react;
 
 @implementation TrueSheetContentView {
   RCTScrollViewComponentView *_pinnedScrollView;
   CGSize _lastSize;
   CGFloat _bottomInset;
   CGFloat _originalScrollViewHeight;
   CGFloat _originalIndicatorBottomInset;
   BOOL _observingTextChanges;
 }
 
+- (CGFloat)resolvedIndicatorBottomInset {
+  return MAX(_originalIndicatorBottomInset, _bottomInset);
+}
+
 + (ComponentDescriptorProvider)componentDescriptorProvider {
   return concreteComponentDescriptorProvider<TrueSheetContentViewComponentDescriptor>();
 }
 
 - (instancetype)initWithFrame:(CGRect)frame {
   if (self = [super initWithFrame:frame]) {
     static const auto defaultProps = std::make_shared<const TrueSheetContentViewProps>();
     _props = defaultProps;
+    _registeredScrollViewTag = -1;
   }
   return self;
 }
 
 - (void)dealloc {
   [self stopObservingTextChanges];
 }
 
 #pragma mark - Text Change Observing
 
 // The notification is app-wide (object:nil), so only observe while the keyboard is up
 - (void)startObservingTextChanges {
   if (_observingTextChanges) {
     return;
   }
   _observingTextChanges = YES;
   [[NSNotificationCenter defaultCenter] addObserver:self
                                            selector:@selector(focusedInputTextDidChange:)
                                                name:UITextViewTextDidChangeNotification
                                              object:nil];
 }
 
 - (void)stopObservingTextChanges {
   if (!_observingTextChanges) {
     return;
   }
   _observingTextChanges = NO;
   [[NSNotificationCenter defaultCenter] removeObserver:self name:UITextViewTextDidChangeNotification object:nil];
 }
 
 #pragma mark - Layout
 
 - (void)updateLayoutMetrics:(const LayoutMetrics &)layoutMetrics
            oldLayoutMetrics:(const LayoutMetrics &)oldLayoutMetrics {
   [super updateLayoutMetrics:layoutMetrics oldLayoutMetrics:oldLayoutMetrics];
 
   CGSize newSize = CGSizeMake(layoutMetrics.frame.size.width, layoutMetrics.frame.size.height);
   if (!CGSizeEqualToSize(newSize, _lastSize)) {
     _lastSize = newSize;
     [self.delegate contentViewDidChangeSize:newSize];
   }
 }
 
 #pragma mark - Child Mounting
 
 - (void)mountChildComponentView:(UIView<RCTComponentViewProtocol> *)childComponentView index:(NSInteger)index {
   [super mountChildComponentView:childComponentView index:index];
   [self checkScrollViewChanged];
 }
 
 - (void)unmountChildComponentView:(UIView<RCTComponentViewProtocol> *)childComponentView index:(NSInteger)index {
   [super unmountChildComponentView:childComponentView index:index];
   [self checkScrollViewChanged];
 }
 
 - (void)checkScrollViewChanged {
   if (!_pinnedScrollView || ![_pinnedScrollView isDescendantOfView:self]) {
     [self.delegate contentViewScrollViewDidChange];
   }
 }
 
 #pragma mark - Scrollable
 
 - (void)setScrollViewContentInset:(CGFloat)contentBottom indicatorInset:(CGFloat)indicatorBottom {
   if (!_pinnedScrollView)
     return;
 
   UIEdgeInsets contentInset = _pinnedScrollView.scrollView.contentInset;
   contentInset.bottom = contentBottom;
   _pinnedScrollView.scrollView.contentInset = contentInset;
 
   UIEdgeInsets indicatorInsets = _pinnedScrollView.scrollView.verticalScrollIndicatorInsets;
   indicatorInsets.bottom = indicatorBottom;
   _pinnedScrollView.scrollView.verticalScrollIndicatorInsets = indicatorInsets;
 }
 
 - (void)clearScrollable {
   if (_pinnedScrollView) {
     CGRect frame = _pinnedScrollView.frame;
     frame.size.height = _originalScrollViewHeight;
     _pinnedScrollView.frame = frame;
 
     [self setScrollViewContentInset:0 indicatorInset:_originalIndicatorBottomInset];
   }
   _pinnedScrollView = nil;
   _bottomInset = 0;
   _originalScrollViewHeight = 0;
   _originalIndicatorBottomInset = 0;
 }
 
+- (void)setRegisteredScrollViewTag:(NSInteger)registeredScrollViewTag {
+  if (_registeredScrollViewTag == registeredScrollViewTag) {
+    return;
+  }
+
+  [self clearScrollable];
+  _registeredScrollViewTag = registeredScrollViewTag;
+}
+
 - (void)setupScrollable:(BOOL)enabled bottomInset:(CGFloat)bottomInset {
   if (!enabled) {
     [self clearScrollable];
     return;
   }
 
   // Check if pinned scroll view is still valid (still in view hierarchy)
   if (_pinnedScrollView && ![_pinnedScrollView isDescendantOfView:self]) {
     [self clearScrollable];
   }
 
   // Already set up with same inset and valid scroll view
   if (_pinnedScrollView && _bottomInset == bottomInset) {
     return;
   }
 
   RCTScrollViewComponentView *scrollView = [self findScrollView];
   if (!scrollView) {
     return;
   }
 
   // Only capture originals on first pin
   if (!_pinnedScrollView) {
     _originalScrollViewHeight = scrollView.frame.size.height;
     _originalIndicatorBottomInset = scrollView.scrollView.verticalScrollIndicatorInsets.bottom;
     _pinnedScrollView = scrollView;
   }
 
   _bottomInset = bottomInset;
 
   [self updateScrollViewHeight];
 
-  [self setScrollViewContentInset:_bottomInset indicatorInset:_originalIndicatorBottomInset];
+  [self setScrollViewContentInset:_bottomInset indicatorInset:[self resolvedIndicatorBottomInset]];
 
   // If keyboard is currently showing, re-apply the keyboard inset to the new ScrollView
   CGFloat keyboardHeight = _keyboardObserver ? _keyboardObserver.currentHeight : 0;
   if (keyboardHeight > 0) {
-    [self setScrollViewContentInset:keyboardHeight indicatorInset:_originalIndicatorBottomInset + keyboardHeight];
+    [self setScrollViewContentInset:keyboardHeight
+                     indicatorInset:[self resolvedIndicatorBottomInset] + keyboardHeight];
+  }
+
+  // Explicitly registered ScrollViews can be mounted after the sheet has already reached its
+  // initial detent. Their wrapper frame is resized above, but UIKit has not yet laid out the
+  // inner UIScrollView using that final frame, so automatic scroll-indicator safe-area
+  // adjustment is stale until the sheet moves. Mirror the layout pass that a detent change
+  // would trigger without changing any inset values.
+  if (_registeredScrollViewTag >= 0) {
+    __weak TrueSheetContentView *weakSelf = self;
+    __weak RCTScrollViewComponentView *weakScrollView = scrollView;
+    dispatch_async(dispatch_get_main_queue(), ^{
+      TrueSheetContentView *strongSelf = weakSelf;
+      RCTScrollViewComponentView *registeredScrollView = weakScrollView;
+      if (!strongSelf || !registeredScrollView ||
+          strongSelf->_pinnedScrollView != registeredScrollView ||
+          ![registeredScrollView isDescendantOfView:strongSelf]) {
+        return;
+      }
+
+      [registeredScrollView setNeedsLayout];
+      [registeredScrollView layoutIfNeeded];
+      [registeredScrollView.scrollView setNeedsLayout];
+      [registeredScrollView.scrollView layoutIfNeeded];
+    });
   }
 }
 
 - (void)updateScrollViewHeight {
   if (!_pinnedScrollView) {
     return;
   }
 
   UIView *containerView = self.superview;
   if (!containerView) {
     return;
   }
 
   CGRect scrollViewFrameInContainer = [_pinnedScrollView.superview convertRect:_pinnedScrollView.frame
                                                                         toView:containerView];
   CGFloat newHeight = containerView.bounds.size.height - scrollViewFrameInContainer.origin.y;
 
   if (newHeight > 0) {
     // Preserve contentOffset before changing frame to prevent scroll jump
     CGPoint savedContentOffset = _pinnedScrollView.scrollView.contentOffset;
 
     CGRect frame = _pinnedScrollView.frame;
     frame.size.height = newHeight;
     _pinnedScrollView.frame = frame;
 
     // Restore contentOffset after frame change
     _pinnedScrollView.scrollView.contentOffset = savedContentOffset;
   }
 }
 
+- (void)refreshRegisteredScrollViewLayout {
+  if (_registeredScrollViewTag < 0 || !_pinnedScrollView ||
+      ![_pinnedScrollView isDescendantOfView:self]) {
+    return;
+  }
+
+  [self updateScrollViewHeight];
+  [_pinnedScrollView setNeedsLayout];
+  [_pinnedScrollView layoutIfNeeded];
+  [_pinnedScrollView.scrollView setNeedsLayout];
+  [_pinnedScrollView.scrollView layoutIfNeeded];
+}
+
 - (RCTScrollViewComponentView *)findScrollView {
+  if (_registeredScrollViewTag >= 0) {
+    return [self findRegisteredScrollViewInView:self];
+  }
+
   if (_pinnedScrollView) {
     return _pinnedScrollView;
   }
 
   if (self.subviews.count == 0) {
     return nil;
   }
 
   RCTScrollViewComponentView *scrollView = [self findScrollViewInSubviews:self.subviews];
 
   if (!scrollView) {
     for (UIView *subview in self.subviews) {
       scrollView = [self findScrollViewInSubviews:subview.subviews];
       if (scrollView) {
         break;
       }
     }
   }
 
   return scrollView;
 }
 
+- (RCTScrollViewComponentView *)findRegisteredScrollViewInView:(UIView *)view {
+  if (view.tag == _registeredScrollViewTag &&
+      [view isKindOfClass:RCTScrollViewComponentView.class] &&
+      ![view isKindOfClass:TrueSheetView.class]) {
+    return (RCTScrollViewComponentView *)view;
+  }
+
+  for (UIView *subview in view.subviews) {
+    RCTScrollViewComponentView *scrollView = [self findRegisteredScrollViewInView:subview];
+    if (scrollView) {
+      return scrollView;
+    }
+  }
+
+  return nil;
+}
+
 - (RCTScrollViewComponentView *)findScrollViewInSubviews:(NSArray<UIView *> *)subviews {
   for (UIView *subview in subviews) {
     if ([subview isKindOfClass:RCTScrollViewComponentView.class] && ![subview isKindOfClass:TrueSheetView.class]) {
       return (RCTScrollViewComponentView *)subview;
     }
   }
   return nil;
 }
 
 #pragma mark - Scroll Edge Effects
 
 - (void)applyScrollEdgeEffects:(nullable ScrollableOptions *)options {
 #if RNTS_IPHONE_OS_VERSION_AVAILABLE(26_0)
   if (!_pinnedScrollView)
     return;
 
   if (@available(iOS 26.0, *)) {
     UIScrollView *scrollView = _pinnedScrollView.scrollView;
     auto topEffect = options ? options.topScrollEdgeEffect : TrueSheetViewTopScrollEdgeEffect::Hidden;
     auto bottomEffect = options ? options.bottomScrollEdgeEffect : TrueSheetViewBottomScrollEdgeEffect::Hidden;
 
     [self applyEdgeEffect:topEffect toEdge:scrollView.topEdgeEffect];
     [self applyEdgeEffect:(TrueSheetViewTopScrollEdgeEffect)bottomEffect toEdge:scrollView.bottomEdgeEffect];
   }
 #endif
 }
 
 #if RNTS_IPHONE_OS_VERSION_AVAILABLE(26_0)
 - (void)applyEdgeEffect:(TrueSheetViewTopScrollEdgeEffect)effect
                  toEdge:(UIScrollEdgeEffect *)edgeEffect API_AVAILABLE(ios(26.0)) {
   switch (effect) {
     case TrueSheetViewTopScrollEdgeEffect::Automatic:
       edgeEffect.hidden = NO;
       edgeEffect.style = UIScrollEdgeEffectStyle.automaticStyle;
       break;
     case TrueSheetViewTopScrollEdgeEffect::Hard:
       edgeEffect.hidden = NO;
       edgeEffect.style = UIScrollEdgeEffectStyle.hardStyle;
       break;
     case TrueSheetViewTopScrollEdgeEffect::Soft:
       edgeEffect.hidden = NO;
       edgeEffect.style = UIScrollEdgeEffectStyle.softStyle;
       break;
     case TrueSheetViewTopScrollEdgeEffect::Hidden:
       edgeEffect.hidden = YES;
       break;
   }
 }
 #endif
 
 #pragma mark - TrueSheetKeyboardObserverDelegate
 
 - (void)keyboardWillShow:(CGFloat)height duration:(NSTimeInterval)duration curve:(UIViewAnimationOptions)curve {
   if (!_pinnedScrollView) {
     return;
   }
 
   [self startObservingTextChanges];
 
   TrueSheetViewController *sheetController = _keyboardObserver.viewController;
   UIView *firstResponder = sheetController ? [sheetController.view findFirstResponder] : nil;
 
   [UIView animateWithDuration:duration
                         delay:0
                       options:curve | UIViewAnimationOptionBeginFromCurrentState
                    animations:^{
                      [self setScrollViewContentInset:height
-                                      indicatorInset:self->_originalIndicatorBottomInset + height];
+                                      indicatorInset:[self resolvedIndicatorBottomInset] + height];
                    }
                    completion:nil];
 
   // Defer scroll until the next run loop so content insets are applied first
   if (firstResponder) {
     dispatch_async(dispatch_get_main_queue(), ^{
       [self scrollToFocusedCaretAnimated:YES];
     });
   }
 }
 
 - (void)focusedInputTextDidChange:(NSNotification *)notification {
   if (!_pinnedScrollView || !_keyboardObserver || _keyboardObserver.currentHeight <= 0) {
     return;
   }
 
   TrueSheetViewController *sheetController = _keyboardObserver.viewController;
   UIView *firstResponder = sheetController ? [sheetController.view findFirstResponder] : nil;
   if (!firstResponder || notification.object != firstResponder) {
     return;
   }
 
   dispatch_async(dispatch_get_main_queue(), ^{
     [self scrollToFocusedCaretAnimated:YES];
   });
 }
 
 - (void)scrollToFocusedCaretAnimated:(BOOL)animated {
   if (!_pinnedScrollView) {
     return;
   }
 
   TrueSheetViewController *sheetController = _keyboardObserver.viewController;
   UIView *firstResponder = sheetController ? [sheetController.view findFirstResponder] : nil;
   if (!firstResponder) {
     return;
   }
 
   UIScrollView *scrollView = _pinnedScrollView.scrollView;
   CGRect targetRect = [firstResponder convertRect:firstResponder.bounds toView:scrollView];
 
   if ([firstResponder conformsToProtocol:@protocol(UITextInput)]) {
     id<UITextInput> textInput = (id<UITextInput>)firstResponder;
     UITextRange *selectedRange = textInput.selectedTextRange;
     if (selectedRange) {
       CGRect caretRect = [textInput caretRectForPosition:selectedRange.end];
       // caretRectForPosition: can return non-finite coordinates during layout/selection transitions
       BOOL caretRectValid = !CGRectIsNull(caretRect) && !CGRectIsInfinite(caretRect) && isfinite(caretRect.origin.x) &&
                             isfinite(caretRect.origin.y) && isfinite(caretRect.size.width) &&
                             isfinite(caretRect.size.height);
       if (caretRectValid) {
         targetRect = [firstResponder convertRect:caretRect toView:scrollView];
       }
     }
   }
 
   targetRect.size.height += self.keyboardScrollOffset;
   [scrollView scrollRectToVisible:targetRect animated:animated];
 }
 
 - (void)keyboardWillHide:(NSTimeInterval)duration curve:(UIViewAnimationOptions)curve {
   [self stopObservingTextChanges];
 
   if (!_pinnedScrollView) {
     return;
   }
 
   [UIView animateWithDuration:duration
                         delay:0
                       options:curve | UIViewAnimationOptionBeginFromCurrentState
                    animations:^{
                      [self setScrollViewContentInset:self->_bottomInset
-                                      indicatorInset:self->_originalIndicatorBottomInset];
+                                      indicatorInset:[self resolvedIndicatorBottomInset]];
                    }
                    completion:nil];
 }
 
 #pragma mark - Lifecycle
 
 - (void)prepareForRecycle {
   [super prepareForRecycle];
   [self stopObservingTextChanges];
   [self clearScrollable];
 }
 
 @end
 
 Class<RCTComponentViewProtocol> TrueSheetContentViewCls(void) {
   return TrueSheetContentView.class;
 }
 
 #endif
diff --git a/ios/core/TrueSheetGrabberView.mm b/ios/core/TrueSheetGrabberView.mm
index cb24e9c..1a84160 100644
--- a/ios/core/TrueSheetGrabberView.mm
+++ b/ios/core/TrueSheetGrabberView.mm
@@ -22,8 +22,8 @@ - (instancetype)init {
 static const CGFloat kDefaultGrabberWidth = 36.0;
 static const CGFloat kDefaultGrabberHeight = 5.0;
 static const CGFloat kDefaultGrabberTopMargin = 5.0;
-static const CGFloat kHitPaddingHorizontal = 20.0;
 static const CGFloat kHitPaddingVertical = 10.0;
+static const CGFloat kMinimumHitHeight = 28.0;
 
 @implementation TrueSheetGrabberView {
   UIVisualEffectView *_vibrancyView;
@@ -134,14 +134,14 @@ - (void)applyConfiguration {
   CGFloat topMargin = [self effectiveTopMargin];
   CGFloat parentWidth = self.superview ? self.superview.bounds.size.width : UIScreen.mainScreen.bounds.size.width;
 
-  CGFloat frameWidth = pillWidth + kHitPaddingHorizontal * 2;
-  CGFloat frameHeight = pillHeight + kHitPaddingVertical * 2;
-  CGFloat frameY = topMargin - kHitPaddingVertical;
+  CGFloat frameWidth = parentWidth;
+  CGFloat frameHeight = MAX(kMinimumHitHeight, pillHeight + topMargin + kHitPaddingVertical);
+  CGFloat frameY = 0;
 
-  self.frame = CGRectMake((parentWidth - frameWidth) / 2.0, frameY, frameWidth, frameHeight);
+  self.frame = CGRectMake(0, frameY, frameWidth, frameHeight);
   self.backgroundColor = UIColor.clearColor;
 
-  CGRect pillRect = CGRectMake(kHitPaddingHorizontal, kHitPaddingVertical, pillWidth, pillHeight);
+  CGRect pillRect = CGRectMake((frameWidth - pillWidth) / 2.0, topMargin, pillWidth, pillHeight);
   _vibrancyView.frame = pillRect;
   _vibrancyView.layer.cornerRadius = [self effectiveCornerRadius];
   _vibrancyView.clipsToBounds = YES;
diff --git a/lib/module/fabric/TrueSheetViewNativeComponent.ts b/lib/module/fabric/TrueSheetViewNativeComponent.ts
index 247df7e..a4eacfe 100644
--- a/lib/module/fabric/TrueSheetViewNativeComponent.ts
+++ b/lib/module/fabric/TrueSheetViewNativeComponent.ts
@@ -104,6 +104,7 @@ export interface NativeProps extends ViewProps {
   dimmed?: WithDefault<boolean, true>;
   initialDetentAnimated?: WithDefault<boolean, true>;
   scrollable?: WithDefault<boolean, false>;
+  disableStackingTranslation?: WithDefault<boolean, false>;
   scrollableOptions?: ScrollableOptionsType;
   footerOptions?: FooterOptionsType;
   presentation?: WithDefault<'page' | 'form', 'page'>;
diff --git a/lib/typescript/src/TrueSheet.types.d.ts b/lib/typescript/src/TrueSheet.types.d.ts
index dc3d996..0319201 100644
--- a/lib/typescript/src/TrueSheet.types.d.ts
+++ b/lib/typescript/src/TrueSheet.types.d.ts
@@ -367,6 +367,14 @@ export interface TrueSheetProps extends ViewProps {
      * @default 'automatic'
      */
     insetAdjustment?: InsetAdjustment;
+    /**
+     * Disables Android's stacked-sheet translation where a child sheet pushes
+     * the parent sheet downward while both are presented.
+     *
+     * @platform android
+     * @default false
+     */
+    disableStackingTranslation?: boolean;
     /**
      * The elevation (shadow depth) of the sheet.
      *
@@ -573,4 +581,4 @@ export interface TrueSheetStaticMethods {
      */
     dismissAll(animated?: boolean): Promise<void>;
 }
-//# sourceMappingURL=TrueSheet.types.d.ts.map
\ No newline at end of file
+//# sourceMappingURL=TrueSheet.types.d.ts.map
diff --git a/lib/typescript/src/fabric/TrueSheetViewNativeComponent.d.ts b/lib/typescript/src/fabric/TrueSheetViewNativeComponent.d.ts
index 7d0bba2..fb4b48a 100644
--- a/lib/typescript/src/fabric/TrueSheetViewNativeComponent.d.ts
+++ b/lib/typescript/src/fabric/TrueSheetViewNativeComponent.d.ts
@@ -54,6 +54,7 @@ export interface NativeProps extends ViewProps {
     dimmed?: WithDefault<boolean, true>;
     initialDetentAnimated?: WithDefault<boolean, true>;
     scrollable?: WithDefault<boolean, false>;
+    disableStackingTranslation?: WithDefault<boolean, false>;
     scrollableOptions?: ScrollableOptionsType;
     footerOptions?: FooterOptionsType;
     presentation?: WithDefault<'page' | 'form', 'page'>;
@@ -77,4 +78,4 @@ export interface NativeProps extends ViewProps {
 }
 declare const _default: import("react-native").HostComponent<NativeProps>;
 export default _default;
-//# sourceMappingURL=TrueSheetViewNativeComponent.d.ts.map
\ No newline at end of file
+//# sourceMappingURL=TrueSheetViewNativeComponent.d.ts.map
diff --git a/src/TrueSheet.types.ts b/src/TrueSheet.types.ts
index f73484a..c46910e 100644
--- a/src/TrueSheet.types.ts
+++ b/src/TrueSheet.types.ts
@@ -430,6 +430,15 @@ export interface TrueSheetProps extends ViewProps {
    */
   insetAdjustment?: InsetAdjustment;
 
+  /**
+   * Disables Android's stacked-sheet translation where a child sheet pushes
+   * the parent sheet downward while both are presented.
+   *
+   * @platform android
+   * @default false
+   */
+  disableStackingTranslation?: boolean;
+
   /**
    * The elevation (shadow depth) of the sheet.
    *
diff --git a/src/fabric/TrueSheetViewNativeComponent.ts b/src/fabric/TrueSheetViewNativeComponent.ts
index 247df7e..a4eacfe 100644
--- a/src/fabric/TrueSheetViewNativeComponent.ts
+++ b/src/fabric/TrueSheetViewNativeComponent.ts
@@ -104,6 +104,7 @@ export interface NativeProps extends ViewProps {
   dimmed?: WithDefault<boolean, true>;
   initialDetentAnimated?: WithDefault<boolean, true>;
   scrollable?: WithDefault<boolean, false>;
+  disableStackingTranslation?: WithDefault<boolean, false>;
   scrollableOptions?: ScrollableOptionsType;
   footerOptions?: FooterOptionsType;
   presentation?: WithDefault<'page' | 'form', 'page'>;
diff --git a/android/src/main/java/com/lodev09/truesheet/TrueSheetModule.kt b/android/src/main/java/com/lodev09/truesheet/TrueSheetModule.kt
index 533f10d..fd92fac 100644
--- a/android/src/main/java/com/lodev09/truesheet/TrueSheetModule.kt
+++ b/android/src/main/java/com/lodev09/truesheet/TrueSheetModule.kt
@@ -150,6 +150,12 @@ class TrueSheetModule(reactContext: ReactApplicationContext) :
     }
   }
 
+  @ReactMethod
+  fun setScrollableViewByRef(viewTag: Double, scrollViewTag: Double, promise: Promise) {
+    // Explicit ScrollView binding is only needed on iOS. Keep Android behavior unchanged.
+    promise.resolve(null)
+  }
+
   /**
    * Helper method to get TrueSheetView by tag and execute closure
    */
diff --git a/ios/TrueSheetContainerView.h b/ios/TrueSheetContainerView.h
index bd4d453..c510100 100644
--- a/ios/TrueSheetContainerView.h
+++ b/ios/TrueSheetContainerView.h
@@ -60,6 +60,9 @@ NS_ASSUME_NONNULL_BEGIN
  */
 @property (nonatomic, strong, nullable) ScrollableOptions *scrollableOptions;
 
+/** React tag of the explicitly registered ScrollView, or -1 for automatic discovery. */
+@property (nonatomic, assign) NSInteger registeredScrollViewTag;
+
 /**
  * Returns the current content height
  */
@@ -102,6 +105,11 @@ NS_ASSUME_NONNULL_BEGIN
  */
 - (void)setupScrollable;
 
+/**
+ * Re-run layout for the explicitly registered ScrollView without changing its insets.
+ */
+- (void)refreshRegisteredScrollViewLayout;
+
 /**
  * Setup keyboard observer for content and footer
  * @param viewController The sheet view controller to observe keyboard events for
diff --git a/ios/TrueSheetContainerView.mm b/ios/TrueSheetContainerView.mm
index 86dca5d..7c6c82a 100644
--- a/ios/TrueSheetContainerView.mm
+++ b/ios/TrueSheetContainerView.mm
@@ -75,6 +75,7 @@ - (instancetype)initWithFrame:(CGRect)frame {
     _headerView = nil;
     _footerView = nil;
     _scrollableSet = NO;
+    _registeredScrollViewTag = -1;
     self.isAccessibilityElement = NO;
   }
   return self;
@@ -111,6 +112,11 @@ - (BOOL)hasAccessibilityFooterElements {
 
 - (void)layoutSubviews {
   [super layoutSubviews];
+  // Native stacks can mount their active screen after the explicit tag is registered.
+  // Retry discovery during layout until the requested ScrollView becomes available.
+  if (_registeredScrollViewTag >= 0) {
+    [self setupScrollable];
+  }
   [_contentView updateScrollViewHeight];
 }
 
@@ -164,6 +170,11 @@ - (void)setScrollableOptions:(ScrollableOptions *)scrollableOptions {
   _contentView.keyboardScrollOffset = scrollableOptions ? scrollableOptions.keyboardScrollOffset : 0;
 }
 
+- (void)setRegisteredScrollViewTag:(NSInteger)registeredScrollViewTag {
+  _registeredScrollViewTag = registeredScrollViewTag;
+  _contentView.registeredScrollViewTag = registeredScrollViewTag;
+}
+
 - (void)setupScrollable {
   if (_scrollableSet && _contentView) {
     CGFloat bottomInset = 0;
@@ -178,6 +189,11 @@ - (void)setupScrollable {
   }
 }
 
+- (void)refreshRegisteredScrollViewLayout {
+  [self setupScrollable];
+  [_contentView refreshRegisteredScrollViewLayout];
+}
+
 - (void)setupEdgeInteractions API_AVAILABLE(ios(26.0)) {
   if (!_contentView) {
     return;
@@ -214,6 +230,7 @@ - (void)mountChildComponentView:(UIView<RCTComponentViewProtocol> *)childCompone
     }
     _contentView = (TrueSheetContentView *)childComponentView;
     _contentView.delegate = self;
+    _contentView.registeredScrollViewTag = _registeredScrollViewTag;
 
     // Children mount bottom-up, so the content subtree is complete here.
     // Late-mounted peek views attach themselves instead (see TrueSheetPeekView).
diff --git a/ios/TrueSheetContentView.h b/ios/TrueSheetContentView.h
index 45521c7..f91cd13 100644
--- a/ios/TrueSheetContentView.h
+++ b/ios/TrueSheetContentView.h
@@ -32,6 +32,8 @@ NS_ASSUME_NONNULL_BEGIN
 @property (nonatomic, weak, nullable) id<TrueSheetContentViewDelegate> delegate;
 @property (nonatomic, assign) CGFloat keyboardScrollOffset;
 @property (nonatomic, weak, nullable) TrueSheetKeyboardObserver *keyboardObserver;
+/** React tag of the explicitly registered ScrollView, or -1 for automatic discovery. */
+@property (nonatomic, assign) NSInteger registeredScrollViewTag;
 
 - (RCTScrollViewComponentView *_Nullable)findScrollView;
 
@@ -47,6 +49,11 @@ NS_ASSUME_NONNULL_BEGIN
  */
 - (void)updateScrollViewHeight;
 
+/**
+ * Re-run layout for the explicitly registered ScrollView after its native stack transition.
+ */
+- (void)refreshRegisteredScrollViewLayout;
+
 /**
  * Apply scroll edge effects to the pinned scroll view (iOS 26+)
  */
diff --git a/ios/TrueSheetModule.mm b/ios/TrueSheetModule.mm
index 3f2167f..791bda2 100644
--- a/ios/TrueSheetModule.mm
+++ b/ios/TrueSheetModule.mm
@@ -145,6 +145,23 @@ - (void)handleBackPress:(double)viewTag resolve:(RCTPromiseResolveBlock)resolve
   resolve(nil);
 }
 
+- (void)setScrollableViewByRef:(double)viewTag
+                 scrollViewTag:(double)scrollViewTag
+                       resolve:(RCTPromiseResolveBlock)resolve
+                        reject:(RCTPromiseRejectBlock)reject {
+  RCTExecuteOnMainQueue(^{
+    TrueSheetView *trueSheetView = [TrueSheetModule getTrueSheetViewByTag:@((NSInteger)viewTag)];
+
+    if (!trueSheetView) {
+      reject(@"SHEET_NOT_FOUND", [NSString stringWithFormat:@"No sheet found with tag %d", (int)viewTag], nil);
+      return;
+    }
+
+    [trueSheetView setRegisteredScrollViewTag:(NSInteger)scrollViewTag];
+    resolve(nil);
+  });
+}
+
 - (void)dismissAll:(BOOL)animated resolve:(RCTPromiseResolveBlock)resolve reject:(RCTPromiseRejectBlock)reject {
   RCTExecuteOnMainQueue(^{
     @synchronized(viewRegistry) {
diff --git a/ios/TrueSheetView.h b/ios/TrueSheetView.h
index 53d2482..29d87ff 100644
--- a/ios/TrueSheetView.h
+++ b/ios/TrueSheetView.h
@@ -38,6 +38,9 @@ typedef void (^TrueSheetCompletionBlock)(BOOL success, NSError *_Nullable error)
 
 - (void)dismissStackAnimated:(BOOL)animated completion:(nullable TrueSheetCompletionBlock)completion;
 
+/** Bind scrollable integration to a ScrollView React tag, or -1 for automatic discovery. */
+- (void)setRegisteredScrollViewTag:(NSInteger)scrollViewTag;
+
 @end
 
 NS_ASSUME_NONNULL_END
diff --git a/ios/TrueSheetView.mm b/ios/TrueSheetView.mm
index 2ce79bd..118a1d4 100644
--- a/ios/TrueSheetView.mm
+++ b/ios/TrueSheetView.mm
@@ -65,6 +65,7 @@ @implementation TrueSheetView {
   BOOL _pendingPropsUpdate;
   NSArray *_pendingDetents;
   RNScreensEventObserver *_screensEventObserver;
+  NSInteger _registeredScrollViewTag;
 }
 
 #pragma mark - Initialization
@@ -87,6 +88,7 @@ - (instancetype)initWithFrame:(CGRect)frame {
     _initialDetentIndex = -1;
     _initialDetentAnimated = YES;
     _scrollable = NO;
+    _registeredScrollViewTag = -1;
     _isSheetUpdatePending = NO;
 
     _screensEventObserver = [[RNScreensEventObserver alloc] init];
@@ -285,6 +287,19 @@ - (void)updateState:(const State::Shared &)state oldState:(const State::Shared &
   }
 }
 
+- (void)setRegisteredScrollViewTag:(NSInteger)scrollViewTag {
+  if (_registeredScrollViewTag == scrollViewTag) {
+    if (scrollViewTag >= 0) {
+      [_containerView refreshRegisteredScrollViewLayout];
+    }
+    return;
+  }
+
+  _registeredScrollViewTag = scrollViewTag;
+  _containerView.registeredScrollViewTag = scrollViewTag;
+  [self setupScrollable];
+}
+
 /**
  * Updates Fabric state with container dimensions for Yoga layout.
  */
@@ -374,6 +389,7 @@ - (void)mountChildComponentView:(UIView<RCTComponentViewProtocol> *)childCompone
 
   _containerView = (TrueSheetContainerView *)childComponentView;
   _containerView.delegate = self;
+  _containerView.registeredScrollViewTag = _registeredScrollViewTag;
 
   [_touchHandler attachToView:_containerView];
   [_controller.view addSubview:_containerView];
diff --git a/ios/tvos/TrueSheetStubs.mm b/ios/tvos/TrueSheetStubs.mm
index 28bcc66..803d5fa 100644
--- a/ios/tvos/TrueSheetStubs.mm
+++ b/ios/tvos/TrueSheetStubs.mm
@@ -143,6 +143,13 @@ - (void)handleBackPress:(double)viewTag resolve:(RCTPromiseResolveBlock)resolve
   resolve(nil);
 }
 
+- (void)setScrollableViewByRef:(double)viewTag
+                 scrollViewTag:(double)scrollViewTag
+                       resolve:(RCTPromiseResolveBlock)resolve
+                        reject:(RCTPromiseRejectBlock)reject {
+  resolve(nil);
+}
+
 - (void)dismissAll:(BOOL)animated resolve:(RCTPromiseResolveBlock)resolve reject:(RCTPromiseRejectBlock)reject {
   resolve(nil);
 }
diff --git a/lib/module/TrueSheet.js b/lib/module/TrueSheet.js
index 449b9a0..2292870 100644
--- a/lib/module/TrueSheet.js
+++ b/lib/module/TrueSheet.js
@@ -299,6 +299,17 @@ export class TrueSheet extends PureComponent {
   async resize(index) {
     await TrueSheetModule?.resizeByRef(this.handle, index);
   }
+  /**
+   * Explicitly select the native ScrollView used by this sheet's scrollable integration.
+   * Passing null clears the registration and restores automatic discovery.
+   */
+  async setScrollableView(scrollView) {
+    const scrollViewTag = scrollView == null ? -1 : findNodeHandle(scrollView);
+    if (scrollView != null && (scrollViewTag == null || scrollViewTag === -1)) {
+      throw new Error('Could not get native ScrollView tag');
+    }
+    await TrueSheetModule?.setScrollableViewByRef(this.handle, scrollViewTag ?? -1);
+  }
 
   /**
    * Dismiss this sheet and all sheets presented on top of it in a single animation.
@@ -468,4 +479,4 @@ const styles = StyleSheet.create({
     right: 0
   }
 });
-//# sourceMappingURL=TrueSheet.js.map
\ No newline at end of file
+//# sourceMappingURL=TrueSheet.js.map
diff --git a/lib/typescript/src/TrueSheet.d.ts b/lib/typescript/src/TrueSheet.d.ts
index 3aa0625..899a7fe 100644
--- a/lib/typescript/src/TrueSheet.d.ts
+++ b/lib/typescript/src/TrueSheet.d.ts
@@ -1,4 +1,4 @@
-import { PureComponent, type ReactNode } from 'react';
+import { PureComponent, type Component, type ReactNode } from 'react';
 import type { TrueSheetProps, TrueSheetMethods } from './TrueSheet.types';
 interface TrueSheetState {
     shouldRenderNativeView: boolean;
@@ -96,6 +96,11 @@ export declare class TrueSheet extends PureComponent<TrueSheetProps, TrueSheetSt
      * @param index - The detent index to resize to
      */
     resize(index: number): Promise<void>;
+    /**
+     * Explicitly select the native ScrollView used by this sheet's scrollable integration.
+     * Passing null clears the registration and restores automatic discovery.
+     */
+    setScrollableView(scrollView: Component<any, any> | number | null): Promise<void>;
     /**
      * Dismiss this sheet and all sheets presented on top of it in a single animation.
      * @param animated - Whether to animate the dismissal (default: true)
@@ -113,4 +118,4 @@ export declare class TrueSheet extends PureComponent<TrueSheetProps, TrueSheetSt
     render(): ReactNode;
 }
 export {};
-//# sourceMappingURL=TrueSheet.d.ts.map
\ No newline at end of file
+//# sourceMappingURL=TrueSheet.d.ts.map
diff --git a/lib/typescript/src/TrueSheet.types.d.ts b/lib/typescript/src/TrueSheet.types.d.ts
index 0319201..a951825 100644
--- a/lib/typescript/src/TrueSheet.types.d.ts
+++ b/lib/typescript/src/TrueSheet.types.d.ts
@@ -1,4 +1,4 @@
-import type { ComponentType, ReactElement } from 'react';
+import type { Component, ComponentType, ReactElement } from 'react';
 import type { ColorValue, NativeSyntheticEvent, StyleProp, ViewProps, ViewStyle } from 'react-native';
 export interface DetentInfoEventPayload {
     /**
@@ -540,6 +540,11 @@ export interface TrueSheetMethods {
      * @param animated - Whether to animate the dismissal (default: `true`). Ignored on web.
      */
     dismissStack(animated?: boolean): Promise<void>;
+    /**
+     * Explicitly selects the native ScrollView used by the sheet's scrollable integration.
+     * Pass `null` to restore automatic ScrollView discovery.
+     */
+    setScrollableView(scrollView: Component<any, any> | number | null): Promise<void>;
 }
 /**
  * Static methods exposed on the `TrueSheet` component for controlling sheets by `name`.
diff --git a/lib/typescript/src/specs/NativeTrueSheetModule.d.ts b/lib/typescript/src/specs/NativeTrueSheetModule.d.ts
index 098f09c..32026a9 100644
--- a/lib/typescript/src/specs/NativeTrueSheetModule.d.ts
+++ b/lib/typescript/src/specs/NativeTrueSheetModule.d.ts
@@ -38,6 +38,11 @@ interface Spec extends TurboModule {
      * @returns Promise that resolves when resize is complete
      */
     resizeByRef(viewTag: number, index: number): Promise<void>;
+    /**
+     * Explicitly bind the sheet's scrollable behavior to a native ScrollView tag.
+     * Pass -1 to clear the binding and restore automatic discovery.
+     */
+    setScrollableViewByRef(viewTag: number, scrollViewTag: number): Promise<void>;
     /**
      * Dismiss all presented sheets
      * @param animated - Whether to animate the dismissals
@@ -53,4 +58,4 @@ interface Spec extends TurboModule {
 }
 declare const _default: Spec | null;
 export default _default;
-//# sourceMappingURL=NativeTrueSheetModule.d.ts.map
\ No newline at end of file
+//# sourceMappingURL=NativeTrueSheetModule.d.ts.map
diff --git a/src/TrueSheet.tsx b/src/TrueSheet.tsx
index 0a3e82f..ee7c25f 100644
--- a/src/TrueSheet.tsx
+++ b/src/TrueSheet.tsx
@@ -1,6 +1,7 @@
 import {
   PureComponent,
   type RefObject,
+  type Component,
   createRef,
   type ReactNode,
   type ComponentRef,
@@ -401,6 +402,21 @@ export class TrueSheet
     await TrueSheetModule?.resizeByRef(this.handle, index);
   }
 
+  /**
+   * Explicitly select the native ScrollView used by this sheet's scrollable integration.
+   * Passing null clears the registration and restores automatic discovery.
+   */
+  public async setScrollableView(
+    scrollView: Component<any, any> | number | null
+  ): Promise<void> {
+    const scrollViewTag = scrollView == null ? -1 : findNodeHandle(scrollView);
+    if (scrollView != null && (scrollViewTag == null || scrollViewTag === -1)) {
+      throw new Error('Could not get native ScrollView tag');
+    }
+
+    await TrueSheetModule?.setScrollableViewByRef(this.handle, scrollViewTag ?? -1);
+  }
+
   /**
    * Dismiss this sheet and all sheets presented on top of it in a single animation.
    * @param animated - Whether to animate the dismissal (default: true)
diff --git a/src/TrueSheet.types.ts b/src/TrueSheet.types.ts
index c46910e..70174d0 100644
--- a/src/TrueSheet.types.ts
+++ b/src/TrueSheet.types.ts
@@ -1,4 +1,4 @@
-import type { ComponentType, ReactElement } from 'react';
+import type { Component, ComponentType, ReactElement } from 'react';
 import type {
   ColorValue,
   NativeSyntheticEvent,
@@ -632,6 +632,12 @@ export interface TrueSheetMethods {
    * @param animated - Whether to animate the dismissal (default: `true`). Ignored on web.
    */
   dismissStack(animated?: boolean): Promise<void>;
+
+  /**
+   * Explicitly selects the native ScrollView used by the sheet's scrollable integration.
+   * Pass `null` to restore automatic ScrollView discovery.
+   */
+  setScrollableView(scrollView: Component<any, any> | number | null): Promise<void>;
 }
 
 /**
diff --git a/src/specs/NativeTrueSheetModule.ts b/src/specs/NativeTrueSheetModule.ts
index c3a5efb..a1ca216 100644
--- a/src/specs/NativeTrueSheetModule.ts
+++ b/src/specs/NativeTrueSheetModule.ts
@@ -45,6 +45,12 @@ interface Spec extends TurboModule {
    */
   resizeByRef(viewTag: number, index: number): Promise<void>;
 
+  /**
+   * Explicitly bind the sheet's scrollable behavior to a native ScrollView tag.
+   * Pass -1 to clear the binding and restore automatic discovery.
+   */
+  setScrollableViewByRef(viewTag: number, scrollViewTag: number): Promise<void>;
+
   /**
    * Dismiss all presented sheets
    * @param animated - Whether to animate the dismissals
