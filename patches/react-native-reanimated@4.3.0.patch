# Reanimated 补丁说明:
# - 恢复 Expo 55 这一代 Web reduced-motion 回退行为，以及 useReducedMotion 的运行时取值逻辑。
# - Reanimated 版本变化后，需要通过 `upgrade_patched_dependencies` 重新构建此补丁。
diff --git a/src/ReducedMotion.ts b/src/ReducedMotion.ts
index 19f863c..8794e40 100644
--- a/src/ReducedMotion.ts
+++ b/src/ReducedMotion.ts
@@ -6,10 +6,7 @@ type localGlobal = typeof global & Record<string, unknown>;
 
 export function isReducedMotionEnabledInSystem() {
   return IS_WEB
-    ? IS_WINDOW_AVAILABLE
-      ? // @ts-ignore Fallback if `window` is undefined.
-        window.matchMedia('(prefers-reduced-motion: reduce)').matches
-      : false
+    ? false
     : !!(global as localGlobal)._REANIMATED_IS_REDUCED_MOTION;
 }
 
diff --git a/src/hook/useReducedMotion.ts b/src/hook/useReducedMotion.ts
index eeb28cd..23318ca 100644
--- a/src/hook/useReducedMotion.ts
+++ b/src/hook/useReducedMotion.ts
@@ -1,7 +1,5 @@
 'use strict';
-import { isReducedMotionEnabledInSystem } from '../ReducedMotion';
-
-const IS_REDUCED_MOTION_ENABLED_IN_SYSTEM = isReducedMotionEnabledInSystem();
+import { ReducedMotionManager } from '../ReducedMotion';
 
 /**
  * Lets you query the reduced motion system setting.
@@ -14,5 +12,5 @@ const IS_REDUCED_MOTION_ENABLED_IN_SYSTEM = isReducedMotionEnabledInSystem();
  * @see https://docs.swmansion.com/react-native-reanimated/docs/device/useReducedMotion
  */
 export function useReducedMotion() {
-  return IS_REDUCED_MOTION_ENABLED_IN_SYSTEM;
+  return ReducedMotionManager.jsValue;
 }
diff --git a/lib/module/ReducedMotion.js b/lib/module/ReducedMotion.js
index a99fc2c..874bb0a 100644
--- a/lib/module/ReducedMotion.js
+++ b/lib/module/ReducedMotion.js
@@ -3,9 +3,7 @@
 import { IS_WEB, IS_WINDOW_AVAILABLE } from './common';
 import { makeMutable } from './mutables';
 export function isReducedMotionEnabledInSystem() {
-  return IS_WEB ? IS_WINDOW_AVAILABLE ?
-  // @ts-ignore Fallback if `window` is undefined.
-  window.matchMedia('(prefers-reduced-motion: reduce)').matches : false : !!global._REANIMATED_IS_REDUCED_MOTION;
+  return IS_WEB ? false : !!global._REANIMATED_IS_REDUCED_MOTION;
 }
 const IS_REDUCED_MOTION_ENABLED_IN_SYSTEM = isReducedMotionEnabledInSystem();
 export const ReducedMotionManager = {
diff --git a/lib/module/hook/useReducedMotion.js b/lib/module/hook/useReducedMotion.js
index 9c84406..46e96d3 100644
--- a/lib/module/hook/useReducedMotion.js
+++ b/lib/module/hook/useReducedMotion.js
@@ -1,7 +1,6 @@
 'use strict';
 
-import { isReducedMotionEnabledInSystem } from '../ReducedMotion';
-const IS_REDUCED_MOTION_ENABLED_IN_SYSTEM = isReducedMotionEnabledInSystem();
+import { ReducedMotionManager } from '../ReducedMotion';
 
 /**
  * Lets you query the reduced motion system setting.
@@ -14,6 +13,6 @@ const IS_REDUCED_MOTION_ENABLED_IN_SYSTEM = isReducedMotionEnabledInSystem();
  * @see https://docs.swmansion.com/react-native-reanimated/docs/device/useReducedMotion
  */
 export function useReducedMotion() {
-  return IS_REDUCED_MOTION_ENABLED_IN_SYSTEM;
+  return ReducedMotionManager.jsValue;
 }
 //# sourceMappingURL=useReducedMotion.js.map
\ No newline at end of file
